import styled from 'styled-components'

export const NotesContainer = styled.main`
  margin-top: 1rem;
  padding: 0 6rem;

  h2 {
    color: #464646;
    font-size: 0.75rem;
    font-weight: 400;
    margin-bottom: 0.4rem;
    margin-left: 1.2rem;
  }

  ul {
    display: flex;
    flex-wrap: wrap;
    gap: 2rem;
  }
`
